services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    environment:
      - REDIS_ARGS=--requirepass omaressam --appendonly yes
    volumes:
      - redis_data:/data
